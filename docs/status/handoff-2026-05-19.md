# Handoff — sesión 2026-05-19

Punto de retoma para QA manual y trabajo pendiente. **No hay PR ni commit de esta sesión** (cambios locales en la branch de trabajo).

---

## Estado al cerrar hoy

### Hecho en esta iteración (código local)

| Área | Cambio |
|------|--------|
| **Motor premios** | `prize-evaluator.ts` sin N+1; idempotencia en misma bolilla |
| **Wallet** | `wallet-ledger.ts`; migrados purchase, payout, refund, wallet |
| **Seguridad** | `env.ts` (Zod), CORS, rate limit login, auth live draw + JWT display |
| **Live** | `live-session-registry` + bootstrap al arranque; `live-broadcast.ts` (SSE); sin side-effect al import |
| **API bingo** | Rutas modularizadas (`bingo/`, `bingo-rounds.ts`); `bingos.ts` más fino |
| **Compra cartones** | Precarga fingerprints en transacción (menos queries) |
| **Upcoming público** | Cache TTL + invalidación desde CRUD |
| **Shared** | `@backoffice/shared`: escape, money, `connectSseWithReconnect` |
| **Player portal** | `lib/api.ts`, `lib/session.ts`, `lib/format.ts`, `lib/dom.ts`; SSE reconnect |
| **Bingo display** | SSE con reconnect shared |
| **Backoffice** | `bo-escape.js` en admin-pages, room, player, pager, bingo-admin |
| **CI / tests** | `npm test` raíz; 44 tests API + 3 shared; eslint + workflow CI |
| **Docs** | `architecture.md`, `code-quality-improvement-plan.md`, `api/README.md` |

### Última verificación automatizada

```text
npm test  → shared 3 + API unit 41 + API integration 9 + builds
```

Tests reorganizados en `api/tests/` — ver `api/tests/README.md` y `npm run test:list` (desde `api/`).

### Branch / git

- Trabajo en branch tipo `feature/bingo-engine-wallet-players-deferred-prizes` (confirmar con `git branch --show-current`).
- **Sin commit/push pedido** al cerrar hoy.

---

## Mañana — qué hacer primero

1. **Reiniciar API** (`cd api && npm run dev`) para bootstrap de sesiones live y env validado.
2. **`npm test`** en la raíz del repo (sanity check rápido).
3. **Checklist manual** (abajo) — ~15–25 min.
4. Si todo OK → decidir si commit/push o seguir con pendientes (Q7.1 / Q8.3 / Q10).

---

## Pruebas a realizar (checklist QA)

Basado en `docs/status/product-progress.md` § “Checklist de regresión (código)” + cambios de esta sesión.

### Automatizado

- [ ] `npm test` (raíz) — 0 fallos
- [ ] `cd api && npm test` — integración requiere `DATABASE_URL` en `api/.env`

### Manual — prioridad alta

- [ ] **BO:** crear/editar bingo 75 — premios **fijo** y **%** con pozo; figuras LINE + al menos una letra; guardar y reabrir (datos coherentes)
- [ ] **BO:** ver partida programada / agenda de rondas
- [ ] **Portal:** login jugador; comprar **2 cartones** en partida SCHEDULED; saldo descontado correcto
- [ ] **Portal:** pestaña **Mis cartones** — grillas 5×5 visibles
- [ ] **Sorteo virtual:** partida llega a premio línea; wallet acreditado o deferred según configuración del bingo
- [ ] **Display:** SSE muestra bolillas / estado de ronda durante sorteo virtual

### Manual — prioridad media

- [ ] **Portal en vivo:** con partida DRAWING, celdas marcadas en “Mis cartones” (SSE por sala)
- [ ] **Display:** countdown, round_start, toast `prize_awarded` si aplica
- [ ] **BO:** cancelar partida programada → reembolso en wallet del jugador
- [ ] **BO:** jugador — listar movimientos (detalle compra/premio)

### Solo si usás sorteo Live

- [ ] Mismo secret en `api/.env` (`BINGO_DISPLAY_DRAW_SECRET`) y `bingo-display/.env` (`VITE_BINGO_DISPLAY_DRAW_SECRET`)
- [ ] Registrar **1 bolilla** desde display; snapshot coherente; sin 401 inesperado
- [ ] En dev: `LIVE_DRAW_AUTH_OPTIONAL=true` si no querés forzar JWT aún

### Opcional (no bloqueante)

- [ ] `SSE_BALL_DELTA=1` en API — ver evento extra `ball_delta` (clientes actuales lo ignoran; default **off**)
- [ ] `UPCOMING_CACHE_TTL_MS=0` — desactivar cache y comparar listado upcoming

### Si algo falla — dónde mirar

| Síntoma | Revisar |
|---------|---------|
| 401 en draw-ball | `BINGO_DISPLAY_DRAW_SECRET`, `LIVE_DRAW_AUTH_OPTIONAL`, JWT display |
| Saldo no baja / no sube | `wallet-ledger`, `carton-purchase`, `prize-payout` |
| Premio duplicado o no paga | `prize-evaluator.integration.test.ts` + logs API `afterBall` |
| SSE cortado | reiniciar API; `live-broadcast` / reconnect en portal y display |
| Upcoming desactualizado | cache 10s default; invalidar creando/editando bingo |

---

## Variables de entorno relevantes (`api/.env`)

| Variable | Notas |
|----------|--------|
| `DATABASE_URL` | Obligatorio |
| `JWT_SECRET` | ≥ 16 caracteres |
| `BINGO_DISPLAY_DRAW_SECRET` | Live draw (mismo valor en display Vite) |
| `LIVE_DRAW_AUTH_OPTIONAL` | Default relajado fuera de `production` |
| `CORS_ORIGINS` | Orígenes BO / portal / display |
| `UPCOMING_CACHE_TTL_MS` | Default `10000`; `0` = sin cache |
| `SSE_BALL_DELTA` | Default off |

Reiniciar API tras cambiar `.env`.

---

## Pendientes del plan (después del QA)

Referencia completa: `docs/roadmap/code-quality-improvement-plan.md`

| ID | Tarea | Riesgo | Nota |
|----|--------|--------|------|
| **Q7.1** | Partir `live-session.ts` (scheduler vs lifecycle) | Alto | Solo después de virtual + live manual OK |
| **Q7.2** | Capa repo en prize-evaluator | Medio | |
| **Q8.1** | Limpiar scripts demo webpack no usados | Bajo | |
| **Q8.3** | Partir `bingo-admin.js` (~1100 líneas) | Bajo–medio | Solo BO |
| **Q8.4** | Partir `admin-pages.js` (~1400 líneas) | Bajo–medio | users/roles/signin |
| **Q8.5** | Alias `@shared` completo en webpack BO | Bajo | |
| **Q9.2–Q9.3** | Tipos shared + cliente HTTP unificado | Medio | |
| **Q10.1–Q10.2** | Módulos vistas portal/display (`auth`, `buy`, `cards`, `tx`) | Bajo | Ya hay `lib/*` parcial |
| **Q5.3** | Migrar rutas a `next(err)` + error handler | Medio | |
| **Q4.4** | SSE delta — **implementado** con flag `SSE_BALL_DELTA` (off por defecto) | — | |
| **Q4.5** | Spike Redis sesión live multi-instancia | — | Documentación / futuro prod |
| **Q11.3** | Husky pre-commit opcional | Bajo | |
| **Q12.2** | Batch fingerprints compra — **parcial** (precarga Set) | — | |
| **Q12.4–Q12.8** | OpenAPI, métricas, Redis prod, E2E Playwright | Varios | |

### Orden sugerido post-QA

1. Commit/push si el checklist manual pasa (mensaje según convención de branch).
2. **Q10** portal (bajo riesgo) o **Q8.3** BO (solo clicks admin).
3. **Q7.1** cuando sorteos estén validados.

---

## Comandos útiles

```powershell
cd d:\Proyectos\Backoffice\backoffice_default
npm test

cd api
npm run dev

cd ..\player-portal
npm run dev

cd ..\bingo-display
npm run dev

cd ..\backoffice
npm run dev
```

---

## Cómo retomar en el chat

Decir por ejemplo:

> “Retomo desde el handoff del 19/05: quiero la lista de pruebas y pendientes.”

El asistente puede leer este archivo: `docs/status/handoff-2026-05-19.md`.
