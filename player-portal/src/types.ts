export type {
  BingoOccurrence as OccRow,
  CardCell,
  LiveSnapMinimal as LiveSnap,
  PlayerPortalCard as MyCardRow,
  PublicRoom as RoomOpt,
  WalletTxDetail as TxDetail,
} from "@shared/index.ts";

export type PpTab = "buy" | "cards" | "tx" | "deposit";
