# Opcional: URL de la API (por defecto el portal usa http://localhost:4001).
# VITE_API_BASE=http://localhost:4001

# Módulo de pagos — tab Depositar (Fase 1). Usar 0 o false para ocultar.
# VITE_PAYMENTS_ENABLED=1
