/**
 * Familias de motor de juego. Cada familia vive en su propio subdirectorio bajo `game-engine/`.
 * Ej.: `bingo/`, `slots/` (futuro), `crash/` (futuro).
 */
export type GameEngineFamily = "bingo" | "slots" | "crash";
